#pragma once

#include "task2.h"

// Две функции тестов. 
// Тестируют функции, указанные в их названии.
// Возвращают true, если всё хорошо, и false в противном случае.
bool testForCreateAndDeleteSet(void);

bool testForIsTheSameSetAndUnite(void);